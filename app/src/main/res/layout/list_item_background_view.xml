<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/backgroundView"
    android:layout_width="match_parent"
    android:layout_height="@dimen/lock_ui_item_list_item_height"
    android:background="?attr/colorButtonNormal"
    >

    <ImageView
        android:id="@+id/deleteImageView"
        android:layout_width="@dimen/lock_ui_item_list_item_option_icon_width"
        android:layout_height="0dp"
        android:scaleType="center"
        android:src="@drawable/lock_ui_ic_delete"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <ImageView
        android:id="@+id/copyImageView"
        android:layout_width="@dimen/lock_ui_item_list_item_option_icon_width"
        android:layout_height="0dp"
        android:scaleType="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/deleteImageView"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/lock_ui_ic_copy"
        tools:ignore="ContentDescription"
        />

</androidx.constraintlayout.widget.ConstraintLayout>
